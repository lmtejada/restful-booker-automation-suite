# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: api/functional/booking-delete.spec.ts >> delete booking — DELETE /booking/:id >> [TC-028]: deleting an already-deleted booking returns 404, not 405
- Location: tests/api/functional/booking-delete.spec.ts:51:13

# Error details

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: 404
Received: 405
```

# Test source

```ts
  1  | import { expect, test } from '@fixtures/app.fixture';
  2  | 
  3  | import { DEFAULT_BOOKING_DATA } from '@test-data/factories/booking-data.factory';
  4  | 
  5  | test.describe(
  6  |     'delete booking — DELETE /booking/:id',
  7  |     { tag: ['@api', '@regression'] },
  8  |     () => {
  9  |         let bookingId: number;
  10 | 
  11 |         test.beforeEach(async ({ bookingClient }) => {
  12 |             const response = await bookingClient.create(DEFAULT_BOOKING_DATA);
  13 |             const body = await response.json();
  14 |             bookingId = body.bookingid;
  15 |         });
  16 | 
  17 |         test(
  18 |             '[TC-026]: deletes a booking with a valid token, then 404s on lookup',
  19 |             { tag: '@smoke' },
  20 |             async ({ bookingClient, authToken }) => {
  21 |                 const deleteResponse = await bookingClient.delete(
  22 |                     bookingId,
  23 |                     authToken
  24 |                 );
  25 |                 expect(deleteResponse.status()).toBe(201);
  26 | 
  27 |                 const verifyResponse = await bookingClient.getById(bookingId);
  28 |                 expect(verifyResponse.status()).toBe(404);
  29 |             }
  30 |         );
  31 | 
  32 |         test('[TC-027]: rejects a delete request with no authorization', async ({
  33 |             bookingClient,
  34 |         }) => {
  35 |             const response = await bookingClient.delete(bookingId);
  36 | 
  37 |             expect(response.status()).toBe(403);
  38 |         });
  39 | 
  40 |         test('[TC-027]: rejects a delete request with a malformed token', async ({
  41 |             bookingClient,
  42 |         }) => {
  43 |             const response = await bookingClient.delete(
  44 |                 bookingId,
  45 |                 'not-a-real-token'
  46 |             );
  47 | 
  48 |             expect(response.status()).toBe(403);
  49 |         });
  50 | 
  51 |         test(
  52 |             '[TC-028]: deleting an already-deleted booking returns 404, not 405',
  53 |             { tag: '@issues' },
  54 |             async ({ bookingClient, authToken }) => {
  55 |                 test.fail(true, 'Known bug — see docs/DEFECT-LOG.md (BUG-007)');
  56 | 
  57 |                 const firstDelete = await bookingClient.delete(
  58 |                     bookingId,
  59 |                     authToken
  60 |                 );
  61 |                 expect(firstDelete.status()).toBe(201);
  62 | 
  63 |                 const secondDelete = await bookingClient.delete(
  64 |                     bookingId,
  65 |                     authToken
  66 |                 );
> 67 |                 expect(secondDelete.status()).toBe(404);
     |                                               ^ Error: expect(received).toBe(expected) // Object.is equality
  68 |             }
  69 |         );
  70 |     }
  71 | );
  72 | 
```