# Test Cases Suite — Restful Booker API

**Type:** Smoke | Functional | Regression | Integration <br>
**Priority:** 🔴 High | 🟡 Medium | 🟢 Low <br>
**Automated:** Yes / No <br>
**Test Status:** ⬜ Not run | ✅ Pass | ❌ Fail | ⏭ Skipped | 🚧 Blocked <br>

## Content index

- [1. Authentication](#1-authentication)
    - [TC-001 — Valid credentials return an auth token](#tc-001--valid-credentials-return-an-auth-token)
    - [TC-002 — Invalid or incomplete credentials return "Bad credentials" without a token](#tc-002--invalid-or-incomplete-credentials-return-bad-credentials-without-a-token)
    - [TC-003 — Non-JSON Content-Type on /auth returns a misleading "Bad credentials"](#tc-003--non-json-content-type-on-auth-returns-a-misleading-bad-credentials)
    - [TC-004 — Unsupported HTTP method on /auth returns 404](#tc-004--unsupported-http-method-on-auth-returns-404)
- [2. Create Booking](#2-create-booking)
    - [TC-005 — Creates a booking from a valid JSON payload](#tc-005--creates-a-booking-from-a-valid-json-payload)
    - [TC-006 — Creates a booking from an XML payload](#tc-006--creates-a-booking-from-an-xml-payload)
    - [TC-007 — XML response Content-Type is mislabeled as text/html](#tc-007--xml-response-content-type-is-mislabeled-as-texthtml)
    - [TC-008 — Input field validation for booking creation](#tc-008--input-field-validation-for-booking-creation)
    - [TC-009 — Unsupported Content-Type on creation returns a 500 instead of a 4xx](#tc-009--unsupported-content-type-on-creation-returns-a-500-instead-of-a-4xx)
    - [TC-031 — Stores injection-style inputs inertly instead of erroring](#tc-031--stores-injection-style-inputs-inertly-instead-of-erroring)
- [3. Retrieve Bookings](#3-retrieve-bookings)
    - [TC-010 — Lists all booking ids](#tc-010--lists-all-booking-ids)
    - [TC-011 — Filters bookings by name query parameters](#tc-011--filters-bookings-by-name-query-parameters)
    - [TC-012 — Date query filters never match a booking against its own dates](#tc-012--date-query-filters-never-match-a-booking-against-its-own-dates)
    - [TC-013 — Query parameter edge cases](#tc-013--query-parameter-edge-cases)
    - [TC-014 — Name filters match case-sensitively](#tc-014--name-filters-match-case-sensitively)
    - [TC-015 — Gets a single booking by id](#tc-015--gets-a-single-booking-by-id)
    - [TC-016 — Content negotiation via Accept: application/xml for a single booking](#tc-016--content-negotiation-via-accept-applicationxml-for-a-single-booking)
    - [TC-017 — Invalid booking id lookups return 404](#tc-017--invalid-booking-id-lookups-return-404)
    - [TC-018 — Malformed date query filter is silently matched instead of rejected](#tc-018--malformed-date-query-filter-is-silently-matched-instead-of-rejected)
- [4. Update Booking](#4-update-booking)
    - [TC-019 — Fully replaces a booking via PUT with a valid token](#tc-019--fully-replaces-a-booking-via-put-with-a-valid-token)
    - [TC-020 — Partially updates a booking via PATCH, leaving other fields intact](#tc-020--partially-updates-a-booking-via-patch-leaving-other-fields-intact)
    - [TC-021 — Alternative auth methods and precedence on update requests](#tc-021--alternative-auth-methods-and-precedence-on-update-requests)
    - [TC-022 — Rejects a PUT with an empty body instead of wiping the booking](#tc-022--rejects-a-put-with-an-empty-body-instead-of-wiping-the-booking)
    - [TC-023 — Ignores unexpected extra fields on update](#tc-023--ignores-unexpected-extra-fields-on-update)
    - [TC-024 — Rejects update requests with missing or invalid authorization](#tc-024--rejects-update-requests-with-missing-or-invalid-authorization)
    - [TC-025 — Updating a non-existent booking id returns 405 instead of 404](#tc-025--updating-a-non-existent-booking-id-returns-405-instead-of-404)
- [5. Delete Booking](#5-delete-booking)
    - [TC-026 — Deletes a booking with a valid token, then 404s on lookup](#tc-026--deletes-a-booking-with-a-valid-token-then-404s-on-lookup)
    - [TC-027 — Rejects delete requests with missing or invalid authorization](#tc-027--rejects-delete-requests-with-missing-or-invalid-authorization)
    - [TC-028 — Deleting an already-deleted booking returns 405 instead of 404](#tc-028--deleting-an-already-deleted-booking-returns-405-instead-of-404)
- [6. Booking Lifecycle (End-to-End)](#6-booking-lifecycle-end-to-end)
    - [TC-029 — End-to-end booking lifecycle: authenticate → create → read → update → delete](#tc-029--end-to-end-booking-lifecycle-authenticate--create--read--update--delete)

---

## 1. Authentication

### TC-001 — Valid credentials return an auth token

**Feature:** Authentication<br>
**Type:** Functional<br>
**Priority:** 🔴 High<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/auth.spec.ts` — Scenario: "[Smoke] valid credentials return a token"<br>
**Tags:** `@api` `@regression` `@smoke`<br>

**Preconditions:**

- None.

**Test steps:**

| Step | Action                                                                              | Expected result                                               |
| ---- | ----------------------------------------------------------------------------------- | ------------------------------------------------------------- |
| 1    | Send `POST /auth` with valid admin credentials and `Content-Type: application/json` | `200 OK` with a non-empty string `token` in the response body |

**Expected result:**
A valid login returns a usable auth token.

**Actual result:**
Matches expected.

**Test data:**

| Field    | Value                            |
| -------- | -------------------------------- |
| username | `admin` (`ADMIN_USERNAME`)       |
| password | `password123` (`ADMIN_PASSWORD`) |

**Notes:**
This token is reused by the `authToken` worker-scoped fixture (`src/fixtures/auth.fixture.ts`) across the update/delete suites instead of re-authenticating per test.

**Status:** ✅ Pass

---

### TC-002 — Invalid or incomplete credentials return "Bad credentials" without a token

**Feature:** Authentication<br>
**Type:** Functional<br>
**Priority:** 🟡 Medium<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/auth.spec.ts` — Scenarios: "rejects an incorrect password" / "rejects an incorrect username" / "rejects a request missing the username field" / "rejects a request missing the password field"<br>
**Tags:** `@api` `@regression`<br>

**Preconditions:**

- None.

**Test steps:**

| Step | Action                                             | Expected result                                                |
| ---- | -------------------------------------------------- | -------------------------------------------------------------- |
| 1    | Send `POST /auth` with each variation in Test data | `200 OK` with body `{"reason": "Bad credentials"}`, no `token` |

**Expected result:**
None of the malformed/incorrect credential combinations produce a token.

**Actual result:**
Matches expected for all four variations.

**Test data:**

| Variation              | Username         | Password         |
| ---------------------- | ---------------- | ---------------- |
| Incorrect password     | `admin`          | `wrong-password` |
| Incorrect username     | `new-user`       | `password123`    |
| Missing username field | _(field absent)_ | `password123`    |
| Missing password field | `admin`          | _(field absent)_ |

**Notes:**
The API returns `200`, not `401`/`403`, for bad credentials — this is intentional API behavior, not a defect (see BUG-010 for the one credentials-adjacent case that _is_ a defect).

**Status:** ✅ Pass

---

### TC-003 — Non-JSON Content-Type on /auth returns a misleading "Bad credentials"

**Feature:** Authentication<br>
**Type:** Functional<br>
**Priority:** 🟢 Low<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/auth.spec.ts` — Scenario: "returns 415 for a non-JSON Content-Type instead of a misleading Bad credentials"<br>
**Tags:** `@api` `@regression` `@issues`<br>

**Preconditions:**

- None.

**Test steps:**

| Step | Action                                                                      | Expected result                         |
| ---- | --------------------------------------------------------------------------- | --------------------------------------- |
| 1    | Send `POST /auth` with **valid** credentials but `Content-Type: text/plain` | `415 Unsupported Media Type` (or `400`) |

**Expected result:**
A wrong Content-Type should be rejected as a malformed request, not conflated with wrong credentials.

**Actual result:**
Fails as expected (wrapped in `test.fail()`) — API returns `200` with `{"reason":"Bad credentials"}` even though the credentials are correct, misleading the caller. Logged as **BUG-010**.

**Test data:**

| Field        | Value                               |
| ------------ | ----------------------------------- |
| Content-Type | `text/plain`                        |
| Body         | Valid credentials, JSON-stringified |

**Notes:**
See `docs/4. DEFECT-LOG.md` (BUG-010).

**Status:** ❌ Fail _(expected — documents a known bug)_

---

### TC-004 — Unsupported HTTP method on /auth returns 404

**Feature:** Authentication<br>
**Type:** Functional<br>
**Priority:** 🟢 Low<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/auth.spec.ts` — Scenario: "rejects unsupported HTTP methods"<br>
**Tags:** `@api` `@regression`<br>

**Preconditions:**

- None.

**Test steps:**

| Step | Action           | Expected result |
| ---- | ---------------- | --------------- |
| 1    | Send `GET /auth` | `404 Not Found` |

**Expected result:**
Only `POST` is a valid method for this route.

**Actual result:**
Matches expected.

**Test data:**

| Field  | Value |
| ------ | ----- |
| Method | `GET` |

**Status:** ✅ Pass

---

## 2. Create Booking

### TC-005 — Creates a booking from a valid JSON payload

**Feature:** Create Booking<br>
**Type:** Functional<br>
**Priority:** 🔴 High<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/booking-create.spec.ts` — Scenario: "[Smoke] creates a booking from a valid JSON payload"<br>
**Tags:** `@api` `@regression` `@smoke`<br>

**Preconditions:**

- None (no auth required to create a booking).

**Test steps:**

| Step | Action                                                   | Expected result                                                           |
| ---- | -------------------------------------------------------- | ------------------------------------------------------------------------- |
| 1    | Send `POST /booking` with a complete, valid JSON payload | `200 OK`, response has a `bookingid` and echoes back the submitted fields |

**Expected result:**
A well-formed booking is created and its data is returned as submitted.

**Actual result:**
Matches expected.

**Test data:**

| Field           | Value                                                                |
| --------------- | -------------------------------------------------------------------- |
| firstname       | random (faker)                                                       |
| lastname        | random (faker)                                                       |
| totalprice      | random int 100–500 (faker)                                           |
| depositpaid     | random boolean (faker)                                               |
| bookingdates    | future checkin/checkout dates (faker)                                |
| additionalneeds | one of `Breakfast`/`Late Checkout`/`Extra Pillows`/`Airport Shuttle` |

**Status:** ✅ Pass

---

### TC-006 — Creates a booking from an XML payload

**Feature:** Create Booking<br>
**Type:** Functional<br>
**Priority:** 🟡 Medium<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/booking-create.spec.ts` — Scenario: "creates a booking from an XML payload"<br>
**Tags:** `@api` `@regression`<br>

**Preconditions:**

- None.

**Test steps:**

| Step | Action                                                                                     | Expected result                                                                      |
| ---- | ------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------ |
| 1    | Send `POST /booking` with an XML body, `Content-Type: text/xml`, `Accept: application/xml` | `200 OK`, body is XML wrapped in `<created-booking>` containing the submitted fields |

**Expected result:**
Content negotiation for XML creation works end to end for the response body.

**Actual result:**
Matches expected for the body. (The response's `Content-Type` header itself is wrong — see TC-007 / BUG-008.)

**Test data:**

| Field        | Value                        |
| ------------ | ---------------------------- |
| Content-Type | `text/xml`                   |
| Accept       | `application/xml`            |
| Body         | Valid booking, XML-formatted |

**Status:** ✅ Pass

---

### TC-007 — XML response Content-Type is mislabeled as text/html

**Feature:** Create Booking<br>
**Type:** Functional<br>
**Priority:** 🟢 Low<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/booking-create.spec.ts` — Scenario: "returns Content-Type: application/xml for an XML response"<br>
**Tags:** `@api` `@regression` `@issues`<br>

**Preconditions:**

- None.

**Test steps:**

| Step | Action                                                                        | Expected result                                     |
| ---- | ----------------------------------------------------------------------------- | --------------------------------------------------- |
| 1    | Send `POST /booking` with `Content-Type: text/xml`, `Accept: application/xml` | Response `Content-Type` header is `application/xml` |

**Expected result:**
A client trusting the response header (rather than sniffing the body) should be told it's XML.

**Actual result:**
Fails as expected — header reads `text/html; charset=utf-8` despite a well-formed XML body. Logged as **BUG-008**.

**Notes:**
See `docs/4. DEFECT-LOG.md` (BUG-008). Same defect also reproduces on `GET /booking/:id` — see TC-016.

**Status:** ❌ Fail _(expected — documents a known bug)_

---

### TC-008 — Input field validation for booking creation

**Feature:** Create Booking<br>
**Type:** Functional<br>
**Priority:** 🔴 High<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/booking-validation.spec.ts` — data-driven `Validation: <description>` scenarios; `tests/api/functional/booking-create.spec.ts` — Scenarios: "accepts an illogical date range where checkin is after checkout" / "rejects syntactically malformed JSON with a 400" / "returns a 500 for a completely empty body instead of a 400" / "ignores unexpected extra fields in the payload"<br>
**Tags:** `@api` `@regression` `@issues` (per-variation)<br>

**Preconditions:**

- None.

**Test steps:**

| Step | Action                                                         | Expected result                          |
| ---- | -------------------------------------------------------------- | ---------------------------------------- |
| 1    | Send `POST /booking` with the payload variation from Test data | See "Expected status" per variation      |
| 2    | Compare the actual response against the expected one           | Matches, or is flagged as a known defect |

**Expected result:**
Every structurally or semantically invalid payload is rejected with `400`; valid edge-case payloads succeed with `200` and behave predictably.

**Actual result:**
The automated suite passes (green CI): 13 of the 24 variations below are wrapped in `test.fail()` because the API's real behavior diverges from the expected one — each is a documented, intentionally-failing assertion, not an unexpected failure. See `docs/4. DEFECT-LOG.md` for full repro steps.

**Test data:**

| Variation                                         | Expected status     | Actual status                     | Defect  |
| ------------------------------------------------- | ------------------- | --------------------------------- | ------- |
| Missing `firstname`                               | 400                 | 500                               | BUG-001 |
| Missing `lastname`                                | 400                 | 500                               | BUG-001 |
| Null `lastname` value                             | 400                 | 500                               | BUG-001 |
| Empty string for `firstname` and `lastname`       | 400                 | 200 (created with blank names)    | BUG-002 |
| Missing `totalprice`                              | 400                 | 500                               | BUG-001 |
| Non-numeric `totalprice` (`"one-hundred"`)        | 400                 | 200 (stored as-is)                | BUG-004 |
| Floating-point `totalprice` (`99.5`)              | 400                 | 200 (silently truncated to `99`)  | BUG-004 |
| Negative `totalprice` (`-100`)                    | 400                 | 200 (accepted)                    | BUG-004 |
| Zero `totalprice` (`0`)                           | 400                 | 200 (accepted)                    | BUG-004 |
| Missing `depositpaid`                             | 400                 | 500                               | BUG-001 |
| Non-boolean `depositpaid` (`"yes"`)               | 400                 | 200 (stored as-is)                | BUG-004 |
| Missing `bookingdates`                            | 400                 | 500                               | BUG-001 |
| Missing `bookingdates.checkin`                    | 400                 | 500                               | BUG-001 |
| Missing `bookingdates.checkout`                   | 400                 | 500                               | BUG-001 |
| Invalid date format for `checkin`                 | 400                 | 200 (corrupted to `"0NaN-aN-aN"`) | BUG-003 |
| Invalid date format for `checkout`                | 400                 | 200 (corrupted to `"0NaN-aN-aN"`) | BUG-003 |
| Illogical date range (`checkin` after `checkout`) | 400                 | 200 (accepted as-is)              | BUG-005 |
| Empty booking payload (all fields `undefined`)    | 400                 | 500                               | BUG-001 |
| Completely absent request body                    | 400                 | 500                               | BUG-001 |
| Syntactically malformed JSON body                 | 400                 | 400 ✅ (correctly rejected)       | —       |
| Extra unexpected field (`isAdmin: true`)          | 200 (field ignored) | 200, field dropped ✅             | —       |
| Null `additionalneeds` value                      | 200                 | 200 ✅                            | —       |
| Empty string for `additionalneeds`                | 200                 | 200 ✅                            | —       |
| `additionalneeds` with special characters         | 200                 | 200 ✅                            | —       |

**Notes:**
The last four rows are deliberately permissive by design (`additionalneeds` is optional/free-text; extra fields should be safely ignored) — not defects.

**Status:** ✅ Pass _(suite is green; failing-as-expected rows are intentional)_

---

### TC-009 — Unsupported Content-Type on creation returns a 500 instead of a 4xx

**Feature:** Create Booking<br>
**Type:** Functional<br>
**Priority:** 🔴 High<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/booking-create.spec.ts` — Scenarios: "returns a 500 for an unsupported Content-Type instead of a 4xx" / "returns a 500 for a text/plain Content-Type instead of a 4xx"<br>
**Tags:** `@api` `@regression` `@issues`<br>

**Preconditions:**

- None.

**Test steps:**

| Step | Action                                                                                     | Expected result              |
| ---- | ------------------------------------------------------------------------------------------ | ---------------------------- |
| 1    | Send `POST /booking` with the Content-Type from Test data and a body that doesn't match it | `415 Unsupported Media Type` |

**Expected result:**
An unrecognized Content-Type should be rejected cleanly, not crash the server.

**Actual result:**
Fails as expected for both variations — API returns `500 Internal Server Error`. Logged as **BUG-006**.

**Test data:**

| Content-Type       | Body                            |
| ------------------ | ------------------------------- |
| `application/yaml` | `firstname: Jim`                |
| `text/plain`       | Valid booking, JSON-stringified |

**Status:** ❌ Fail _(expected — documents a known bug)_

---

### TC-031 — Stores injection-style inputs inertly instead of erroring

**Feature:** Create Booking<br>
**Type:** Functional<br>
**Priority:** 🟢 Low<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/booking-create.spec.ts` — Scenario: "stores injection-style inputs inertly instead of erroring"<br>
**Tags:** `@api` `@regression`<br>

**Preconditions:**

- None.

**Test steps:**

| Step | Action                                                                                | Expected result                                                             |
| ---- | ------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- |
| 1    | Send `POST /booking` with the injection-style value from Test data in the given field | `200 OK`, the field comes back exactly as sent, with nothing run or changed |

**Expected result:**
A SQL-injection-style string or an HTML/script tag should be treated as inert text, not executed or used to corrupt the request.

**Actual result:**
Matches expected for all three variations — each value round-trips unchanged through create and a follow-up `GET`.

**Test data:**

| Field             | Value                              |
| ----------------- | ---------------------------------- |
| `firstname`       | `Robert'); DROP TABLE bookings;--` |
| `lastname`        | `<script>alert(1)</script>`        |
| `additionalneeds` | `<img src=x onerror=alert(1)>`     |

**Notes:**
This only confirms the API itself doesn't choke on or execute these payloads — it says nothing about a downstream consumer that might unsafely render a stored `firstname`/`lastname` into HTML, which is out of scope for this API-only suite.

**Status:** ✅ Pass

---

## 3. Retrieve Bookings

### TC-010 — Lists all booking ids

**Feature:** Retrieve Bookings<br>
**Type:** Functional<br>
**Priority:** 🔴 High<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/booking-retrieve.spec.ts` — Scenario: "[Smoke] lists all booking ids"<br>
**Tags:** `@api` `@regression` `@smoke`<br>

**Preconditions:**

- None.

**Test steps:**

| Step | Action              | Expected result                                    |
| ---- | ------------------- | -------------------------------------------------- |
| 1    | Send `GET /booking` | `200 OK`, array of objects each with a `bookingid` |

**Expected result:**
The full booking id list is retrievable.

**Actual result:**
Matches expected.

**Status:** ✅ Pass

---

### TC-011 — Filters bookings by name query parameters

**Feature:** Retrieve Bookings<br>
**Type:** Functional<br>
**Priority:** 🟡 Medium<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/booking-retrieve.spec.ts` — Scenarios: "filters by firstname" / "filters by lastname" / "filters by firstname and lastname combined"<br>
**Tags:** `@api` `@regression`<br>

**Preconditions:**

- A fixture booking has been created via `beforeAll`.

**Test steps:**

| Step | Action                                                   | Expected result                                        |
| ---- | -------------------------------------------------------- | ------------------------------------------------------ |
| 1    | Send `GET /booking` with the query params from Test data | `200 OK`, result set includes the fixture booking's id |

**Expected result:**
Name-based filtering correctly narrows results to matching bookings.

**Actual result:**
Matches expected for all three variations.

**Test data:**

| Variation            | Query params                    |
| -------------------- | ------------------------------- |
| firstname only       | `firstname=<fixture firstname>` |
| lastname only        | `lastname=<fixture lastname>`   |
| firstname + lastname | both params together            |

**Status:** ✅ Pass

---

### TC-012 — Date query filters never match a booking against its own dates

**Feature:** Retrieve Bookings<br>
**Type:** Functional<br>
**Priority:** 🟡 Medium<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/booking-retrieve.spec.ts` — Scenarios: "filters by checkin and checkout dates" / "filters by checkin alone"<br>
**Tags:** `@api` `@regression` `@issues`<br>

**Preconditions:**

- A fixture booking has been created via `beforeAll`.

**Test steps:**

| Step | Action                                                                                          | Expected result                              |
| ---- | ----------------------------------------------------------------------------------------------- | -------------------------------------------- |
| 1    | Send `GET /booking` with the fixture's own `checkin`/`checkout` as query params (per Test data) | Result set includes the fixture booking's id |

**Expected result:**
A booking should always match a filter built from its own exact dates.

**Actual result:**
Fails as expected for both variations — the fixture is never included, even though it was just created with those exact dates; reproduces on long-standing bookings too. Logged as **BUG-009**.

**Test data:**

| Variation          | Query params                        |
| ------------------ | ----------------------------------- |
| checkin + checkout | both params, fixture's own values   |
| checkin alone      | only `checkin`, fixture's own value |

**Status:** ❌ Fail _(expected — documents a known bug)_

---

### TC-013 — Query parameter edge cases

**Feature:** Retrieve Bookings<br>
**Type:** Functional<br>
**Priority:** 🟢 Low<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/booking-retrieve.spec.ts` — Scenarios: "returns no matches for an empty query parameter value" / "ignores an unknown query parameter"<br>
**Tags:** `@api` `@regression`<br>

**Preconditions:**

- A fixture booking has been created via `beforeAll`.

**Test steps:**

| Step | Action                                                  | Expected result (per variation)    |
| ---- | ------------------------------------------------------- | ---------------------------------- |
| 1    | Send `GET /booking` with the query param from Test data | See "Expected result" column below |

**Expected result:**
An empty filter value matches nothing; an unrecognized param is safely ignored (doesn't error, doesn't exclude valid results).

**Actual result:**
Matches expected for both variations.

**Test data:**

| Variation     | Query params | Expected result                            |
| ------------- | ------------ | ------------------------------------------ |
| Empty value   | `firstname=` | `200`, fixture booking NOT in results      |
| Unknown param | `foo=bar`    | `200`, fixture booking still IS in results |

**Status:** ✅ Pass

---

### TC-014 — Name filters match case-sensitively

**Feature:** Retrieve Bookings<br>
**Type:** Functional<br>
**Priority:** 🟢 Low<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/booking-retrieve.spec.ts` — Scenario: "matches names case-sensitively"<br>
**Tags:** `@api` `@regression`<br>

**Preconditions:**

- A fixture booking has been created via `beforeAll`.

**Test steps:**

| Step | Action                                                            | Expected result                                                   |
| ---- | ----------------------------------------------------------------- | ----------------------------------------------------------------- |
| 1    | Send `GET /booking?firstname=<UPPERCASE variant of fixture name>` | `200`, fixture booking NOT in results (no case-insensitive match) |
| 2    | Send `GET /booking?firstname=<lowercase variant of fixture name>` | `200`, fixture booking NOT in results                             |

**Expected result:**
Documents that the filter is an exact, case-sensitive match — not a defect, just a behavior worth knowing.

**Actual result:**
Matches expected — neither variant matches.

**Notes:**
See `docs/1. API-OVERVIEW.md` §5 ("looks like a bug, isn't a bug").

**Status:** ✅ Pass

---

### TC-015 — Gets a single booking by id

**Feature:** Retrieve Bookings<br>
**Type:** Functional<br>
**Priority:** 🟡 Medium<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/booking-retrieve.spec.ts` — Scenario: "gets a single booking by id"<br>
**Tags:** `@api` `@regression`<br>

**Preconditions:**

- A fixture booking has been created via `beforeAll`.

**Test steps:**

| Step | Action                                  | Expected result                                             |
| ---- | --------------------------------------- | ----------------------------------------------------------- |
| 1    | Send `GET /booking/:id` for the fixture | `200 OK`, body matches the fixture's `firstname`/`lastname` |

**Expected result:**
A single existing booking is retrievable by id.

**Actual result:**
Matches expected.

**Status:** ✅ Pass

---

### TC-016 — Content negotiation via Accept: application/xml for a single booking

**Feature:** Retrieve Bookings<br>
**Type:** Functional<br>
**Priority:** 🟡 Medium<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/booking-retrieve.spec.ts` — Scenarios: "honors Accept: application/xml for a single booking" / "returns Content-Type: application/xml for an XML response"<br>
**Tags:** `@api` `@regression` `@issues` (second scenario)<br>

**Preconditions:**

- A fixture booking has been created via `beforeAll`.

**Test steps:**

| Step | Action                                                 | Expected result                                                   |
| ---- | ------------------------------------------------------ | ----------------------------------------------------------------- |
| 1    | Send `GET /booking/:id` with `Accept: application/xml` | `200 OK`, body is well-formed XML containing the booking's fields |
| 2    | Check the response `Content-Type` header               | `application/xml`                                                 |

**Expected result:**
Both the response body and its declared Content-Type should reflect XML.

**Actual result:**
Step 1 matches expected. Step 2 fails as expected — header reads `text/html; charset=utf-8`. Logged as **BUG-008** (same defect as TC-007, on a different endpoint).

**Status:** ❌ Fail _(expected for step 2 — documents a known bug; step 1 passes)_

---

### TC-017 — Invalid booking id lookups return 404

**Feature:** Retrieve Bookings<br>
**Type:** Functional<br>
**Priority:** 🟡 Medium<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/booking-retrieve.spec.ts` — Scenarios: "returns 404 for a booking id that does not exist" / "returns 404 for a non-numeric booking id"<br>
**Tags:** `@api` `@regression`<br>

**Preconditions:**

- None.

**Test steps:**

| Step | Action                                | Expected result |
| ---- | ------------------------------------- | --------------- |
| 1    | Send `GET /booking/:id` per Test data | `404 Not Found` |

**Expected result:**
Both an id that doesn't exist and a non-numeric id are handled gracefully.

**Actual result:**
Matches expected for both variations.

**Test data:**

| Variation               | id       |
| ----------------------- | -------- |
| Non-existent numeric id | `999999` |
| Non-numeric id          | `abc`    |

**Notes:**
See `docs/1. API-OVERVIEW.md` §5 — a stricter API might `400` the non-numeric case before checking existence, but `404` is a defensible, consistent choice; not logged as a defect.

**Status:** ✅ Pass

---

### TC-018 — Malformed date query filter is silently matched instead of rejected

**Feature:** Retrieve Bookings<br>
**Type:** Functional<br>
**Priority:** 🟡 Medium<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/booking-retrieve.spec.ts` — Scenario: "rejects a malformed date filter instead of silently matching"<br>
**Tags:** `@api` `@regression` `@issues`<br>

**Preconditions:**

- None.

**Test steps:**

| Step | Action                                                | Expected result   |
| ---- | ----------------------------------------------------- | ----------------- |
| 1    | Send `GET /booking?checkin=08-25-2026` (wrong format) | `400 Bad Request` |

**Expected result:**
An unparseable date filter should be rejected, not silently interpreted.

**Actual result:**
Fails as expected — API returns `200` with a small, arbitrary non-empty subset of bookings. Logged as **BUG-011**.

**Status:** ❌ Fail _(expected — documents a known bug)_

---

## 4. Update Booking

### TC-019 — Fully replaces a booking via PUT with a valid token

**Feature:** Update Booking<br>
**Type:** Functional<br>
**Priority:** 🔴 High<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/booking-update.spec.ts` — Scenario: "[Smoke] fully replaces a booking via PUT with a valid token"<br>
**Tags:** `@api` `@regression` `@smoke`<br>

**Preconditions:**

- A fixture booking has been created via `beforeEach`; a valid `authToken` is available via the worker fixture.

**Test steps:**

| Step | Action                                                                              | Expected result                                    |
| ---- | ----------------------------------------------------------------------------------- | -------------------------------------------------- |
| 1    | Send `PUT /booking/:id` with a full replacement payload and `Cookie: token=<valid>` | `200 OK`, response matches the new payload exactly |

**Expected result:**
A full update with valid auth replaces every field.

**Actual result:**
Matches expected.

**Status:** ✅ Pass

---

### TC-020 — Partially updates a booking via PATCH, leaving other fields intact

**Feature:** Update Booking<br>
**Type:** Functional<br>
**Priority:** 🟡 Medium<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/booking-update.spec.ts` — Scenario: "partially updates a booking via PATCH, leaving other fields intact"<br>
**Tags:** `@api` `@regression`<br>

**Preconditions:**

- A fixture booking has been created via `beforeEach`; a valid `authToken` is available.

**Test steps:**

| Step | Action                                                            | Expected result                                                  |
| ---- | ----------------------------------------------------------------- | ---------------------------------------------------------------- |
| 1    | Send `PATCH /booking/:id` with only `firstname` and a valid token | `200 OK`, `firstname` updated, `lastname`/`totalprice` unchanged |

**Expected result:**
A partial update only touches the fields provided.

**Actual result:**
Matches expected.

**Status:** ✅ Pass

---

### TC-021 — Alternative auth methods and precedence on update requests

**Feature:** Update Booking<br>
**Type:** Functional<br>
**Priority:** 🟡 Medium<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/booking-update.spec.ts` — Scenarios: "accepts Basic Auth as an alternative to the token cookie" / "succeeds with a valid Cookie token even when a fake Authorization header is also present"<br>
**Tags:** `@api` `@regression`<br>

**Preconditions:**

- A fixture booking has been created via `beforeEach`.

**Test steps:**

| Step | Action                                                       | Expected result (per variation) |
| ---- | ------------------------------------------------------------ | ------------------------------- |
| 1    | Send `PUT /booking/:id` with the auth headers from Test data | `200 OK`                        |

**Expected result:**
Both supported auth mechanisms work, and a valid one is honored even if a second, invalid mechanism is also present.

**Actual result:**
Matches expected for both variations — confirms an inclusive-OR check on the two auth mechanisms.

**Test data:**

| Variation                                  | Headers sent                                                   |
| ------------------------------------------ | -------------------------------------------------------------- |
| Basic Auth alone                           | `Authorization: Basic <valid base64(admin:password123)>`       |
| Valid Cookie + fake Authorization together | `Cookie: token=<valid>` **and** `Authorization: Basic <bogus>` |

**Notes:**
See `docs/1. API-OVERVIEW.md` §5 — not a security hole introduced by this API's implementation, but worth knowing when debugging why a "clearly wrong" header didn't cause a rejection.

**Status:** ✅ Pass

---

### TC-022 — Rejects a PUT with an empty body instead of wiping the booking

**Feature:** Update Booking<br>
**Type:** Functional<br>
**Priority:** 🟡 Medium<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/booking-update.spec.ts` — Scenario: "rejects a PUT with an empty body instead of wiping the booking"<br>
**Tags:** `@api` `@regression`<br>

**Preconditions:**

- A fixture booking has been created via `beforeEach`; a valid `authToken` is available.

**Test steps:**

| Step | Action                                                   | Expected result                |
| ---- | -------------------------------------------------------- | ------------------------------ |
| 1    | Send `PUT /booking/:id` with body `{}` and a valid token | `400 Bad Request`              |
| 2    | Fetch the booking again (`GET /booking/:id`)             | Original `firstname` unchanged |

**Expected result:**
An empty replacement payload is rejected outright, and the resource is left untouched.

**Actual result:**
Matches expected.

**Status:** ✅ Pass

---

### TC-023 — Ignores unexpected extra fields on update

**Feature:** Update Booking<br>
**Type:** Functional<br>
**Priority:** 🟢 Low<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/booking-update.spec.ts` — Scenarios: "ignores unexpected extra fields on PUT" / "ignores unexpected extra fields on PATCH"<br>
**Tags:** `@api` `@regression`<br>

**Preconditions:**

- A fixture booking has been created via `beforeEach`; a valid `authToken` is available.

**Test steps:**

| Step | Action                                                                | Expected result                               |
| ---- | --------------------------------------------------------------------- | --------------------------------------------- |
| 1    | Send the request from Test data with an extra field (`isAdmin: true`) | `200 OK`, response does not contain `isAdmin` |

**Expected result:**
Extra JSON keys are safely dropped, not reflected or acted upon.

**Actual result:**
Matches expected for both verbs.

**Test data:**

| Variation | Method  |
| --------- | ------- |
| PUT       | `PUT`   |
| PATCH     | `PATCH` |

**Status:** ✅ Pass

---

### TC-024 — Rejects update requests with missing or invalid authorization

**Feature:** Update Booking<br>
**Type:** Functional<br>
**Priority:** 🔴 High<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/booking-update.spec.ts` — Scenarios: "rejects a PUT request with no authorization" / "rejects a PATCH request with no authorization" / "rejects a PUT request with a malformed token" / "rejects a PUT request with a fake Authorization header"<br>
**Tags:** `@api` `@regression`<br>

**Preconditions:**

- A fixture booking has been created via `beforeEach`.

**Test steps:**

| Step | Action                          | Expected result |
| ---- | ------------------------------- | --------------- |
| 1    | Send the request from Test data | `403 Forbidden` |

**Expected result:**
No auth, a fake Cookie token, or a fake Authorization header alone are all rejected.

**Actual result:**
Matches expected for all four variations.

**Test data:**

| Variation                       | Method | Auth header sent                            |
| ------------------------------- | ------ | ------------------------------------------- |
| No authorization                | PUT    | _(none)_                                    |
| No authorization                | PATCH  | _(none)_                                    |
| Malformed Cookie token          | PUT    | `Cookie: token=not-a-real-token`            |
| Fake Authorization header alone | PUT    | `Authorization: Basic bm90LWFkbWluOndyb25n` |

**Status:** ✅ Pass

---

### TC-025 — Updating a non-existent booking id returns 405 instead of 404

**Feature:** Update Booking<br>
**Type:** Functional<br>
**Priority:** 🟢 Low<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/booking-update.spec.ts` — Scenario: "updating a non-existent booking id returns 404, not 405"<br>
**Tags:** `@api` `@regression` `@issues`<br>

**Preconditions:**

- A valid `authToken` is available.

**Test steps:**

| Step | Action                                                             | Expected result |
| ---- | ------------------------------------------------------------------ | --------------- |
| 1    | Send `PUT /booking/999999999` (non-existent id) with a valid token | `404 Not Found` |

**Expected result:**
Updating a nonexistent resource should read as "not found."

**Actual result:**
Fails as expected — API returns `405 Method Not Allowed`. Logged as **BUG-007**.

**Status:** ❌ Fail _(expected — documents a known bug)_

---

## 5. Delete Booking

### TC-026 — Deletes a booking with a valid token, then 404s on lookup

**Feature:** Delete Booking<br>
**Type:** Functional<br>
**Priority:** 🔴 High<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/booking-delete.spec.ts` — Scenario: "[Smoke] deletes a booking with a valid token, then 404s on lookup"<br>
**Tags:** `@api` `@regression` `@smoke`<br>

**Preconditions:**

- A fixture booking has been created via `beforeEach`; a valid `authToken` is available.

**Test steps:**

| Step | Action                                        | Expected result |
| ---- | --------------------------------------------- | --------------- |
| 1    | Send `DELETE /booking/:id` with a valid token | `201 Created`   |
| 2    | Send `GET /booking/:id` for the same id       | `404 Not Found` |

**Expected result:**
A deleted booking is gone.

**Actual result:**
Matches expected.

**Notes:**
Restful Booker returns `201` (not `200`/`204`) on a successful `DELETE` — intentional API behavior.

**Status:** ✅ Pass

---

### TC-027 — Rejects delete requests with missing or invalid authorization

**Feature:** Delete Booking<br>
**Type:** Functional<br>
**Priority:** 🔴 High<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/booking-delete.spec.ts` — Scenarios: "rejects a delete request with no authorization" / "rejects a delete request with a malformed token"<br>
**Tags:** `@api` `@regression`<br>

**Preconditions:**

- A fixture booking has been created via `beforeEach`.

**Test steps:**

| Step | Action                                   | Expected result |
| ---- | ---------------------------------------- | --------------- |
| 1    | Send `DELETE /booking/:id` per Test data | `403 Forbidden` |

**Expected result:**
No auth or a fake token are both rejected.

**Actual result:**
Matches expected for both variations.

**Test data:**

| Variation              | Auth header sent                 |
| ---------------------- | -------------------------------- |
| No authorization       | _(none)_                         |
| Malformed Cookie token | `Cookie: token=not-a-real-token` |

**Status:** ✅ Pass

---

### TC-028 — Deleting an already-deleted booking returns 405 instead of 404

**Feature:** Delete Booking<br>
**Type:** Functional<br>
**Priority:** 🟢 Low<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/functional/booking-delete.spec.ts` — Scenario: "deleting an already-deleted booking returns 404, not 405"<br>
**Tags:** `@api` `@regression` `@issues`<br>

**Preconditions:**

- A fixture booking has been created via `beforeEach`; a valid `authToken` is available.

**Test steps:**

| Step | Action                                        | Expected result |
| ---- | --------------------------------------------- | --------------- |
| 1    | Send `DELETE /booking/:id` with a valid token | `201 Created`   |
| 2    | Send `DELETE /booking/:id` again, same id     | `404 Not Found` |

**Expected result:**
Deleting an already-gone resource should read as "not found."

**Actual result:**
Step 1 matches expected. Step 2 fails as expected — API returns `405 Method Not Allowed`. Logged as **BUG-007** (same defect as TC-025).

**Status:** ❌ Fail _(expected for step 2 — documents a known bug)_

---

## 6. Booking Lifecycle (End-to-End)

### TC-029 — End-to-end booking lifecycle: authenticate → create → read → update → delete

**Feature:** Booking Lifecycle<br>
**Type:** Regression<br>
**Priority:** 🔴 High<br>
**Automated:** Yes<br>
**Automation reference:** `tests/api/integration/booking-crud.spec.ts` — all 5 steps<br>
**Tags:** `@api` `@integration`<br>

**Preconditions:**

- None. Steps run serially (`test.describe.serial`) and share state (`bookingId`, `token`) across the sequence.

**Test steps:**

| Step | Action                                                                | Expected result                                                    |
| ---- | --------------------------------------------------------------------- | ------------------------------------------------------------------ |
| 1    | `POST /auth` with valid credentials                                   | `200 OK`, response has a `token`                                   |
| 2    | `POST /booking` with a valid payload                                  | `200 OK`, response has a `bookingid` matching the submitted fields |
| 3    | `GET /booking/:id` for the created booking                            | `200 OK`, fields match what was created                            |
| 4    | `PUT /booking/:id` (full update), then `PATCH /booking/:id` (partial) | Both `200 OK`; each response reflects its respective change        |
| 5    | `DELETE /booking/:id`, then `GET /booking/:id` on the same id         | Delete returns `201`; the follow-up `GET` returns `404`            |

**Expected result:**
A single booking can be taken through its full lifecycle without any step breaking the next.

**Actual result:**
Matches expected — all 5 steps pass in sequence.

**Notes:**
This is the one cross-endpoint regression flow in the suite; every other module tests one endpoint in isolation.

**Status:** ✅ Pass
