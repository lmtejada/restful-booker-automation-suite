# Test Execution Summary — <Project Name>

**Release Recommendation:** ✅ Release / ❌ Do not release / ⚠️ Release with known issues <br>

> Delete this note once filled in. Copy this file to `related/TEST-EXECUTION-SUMMARY.md` (drop `.template`) when starting a real project. Append one section per run — don't overwrite the previous run's numbers, so trends over time stay visible. See [../README.md](../README.md) for how all the docs fit together.

---

## Run: <date>

_Scope: <e.g. full suite, which browsers>._

| Metric                 | Value |
| ---------------------- | ----- |
| Total test cases       |       |
| Passed                 |       |
| Failed                 |       |
| Blocked                |       |
| Skipped                |       |
| Pass rate              |       |
| Critical bugs open     |       |
| High bugs open         |       |
| Release Recommendation |       |

**Notes:**
<anything the raw numbers don't explain on their own — e.g. why every skip is tied to a specific open defect rather than a coverage gap, or why a failure here would mean the automation is wrong rather than a known app bug. If the TC-level rollup differs from raw Playwright execution counts (data-driven scenarios or multi-browser runs multiply one TC into several executions), state both numbers and confirm they're consistent.
